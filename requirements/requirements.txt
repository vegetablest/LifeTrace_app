# LifeTrace 依赖文件
# 
# 请根据你的操作系统选择对应的依赖文件：
# 
# Windows 用户:
#   pip install -r requirements/requirements_windows.txt
# 
# macOS 用户:
#   pip install -r requirements/requirements_macos.txt
# 
# 下面是通用依赖（不包含平台特定依赖）

# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
alembic>=1.10.0

# Screenshot and image processing
mss>=9.0.0
Pillow>=10.0.0
imagehash>=4.3.0
opencv-python>=4.8.0

# OCR processing
rapidocr-onnxruntime
numpy>=1.21.0

# Data processing
pyyaml>=6.0
watchdog>=3.0.0
typer>=0.9.0

# Web interface
jinja2>=3.1.0
python-multipart>=0.0.6

# Utils
psutil>=5.9.0
python-dateutil>=2.8.0

# Command line interface
rich>=13.0.0

# PyTorch (CLIP需要)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers (Hugging Face CLIP模型)
transformers>=4.21.0

# 原版OpenAI CLIP (备选)
#git+https://github.com/openai/CLIP.git

# 数值计算
scipy>=1.9.0

# Embedding models and vector database
sentence-transformers>=2.2.0
chromadb>=0.4.0

# Google Generative AI (optional for generation)
# google-generativeai>=0.3.0

# OpenAI API
openai>=1.0.0

# Environment variables
python-dotenv>=1.0.0

# 可选：中文CLIP支持
# git+https://github.com/OFA-Sys/Chinese-CLIP.git

# Note: ChromaDB is used for vector database functionality
# Cross-encoder for reranking is included in sentence-transformers

# 平台特定依赖请使用对应的 requirements_windows.txt 或 requirements_macos.txt